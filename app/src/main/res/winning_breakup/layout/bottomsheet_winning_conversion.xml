<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

       <variable
           name="viewModel"
           type="com.rummytitans.sdk.cardgame.ui.wallet.winning_conversion.WinningConversionViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        app:behavior_hideable="true"
        app:behavior_peekHeight="0dp"
        android:background="@drawable/bottom_button"
        android:backgroundTint="@color/text_color6"
        android:paddingVertical="16dp">

        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/rubik_medium"
            android:lineSpacingExtra="3sp"
            android:text="Convert your Winnings to Deposits!"
            android:visibility="@{viewModel.conversionStep ==3 ? View.GONE : View.VISIBLE}"
            android:textColor="@color/text_color8"
            android:layout_marginStart="16dp"
            android:textSize="16dp"
            app:layout_constraintBottom_toBottomOf="@+id/btnClose"
            app:layout_constraintEnd_toStartOf="@+id/btnClose"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/btnClose" />

        <ImageView
            android:id="@+id/btnClose"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_cross_new"
            android:layout_marginEnd="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/text_color8" />


        <LinearLayout
            android:id="@+id/lContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/txtTitle">

            <include
                android:id="@+id/in_winning_home"
                layout="@layout/layout_winning_conversion_home"
                app:viewModel="@{viewModel}"
                />

            <include
                android:id="@+id/in_winning_field"
                app:viewModel="@{viewModel}"
                layout="@layout/layout_winning_conversion_field"/>

            <include
                android:id="@+id/in_winning_complete"
                app:viewModel="@{viewModel}"
                layout="@layout/layout_winning_conversion_complete"/>

        </LinearLayout>

        <include
            android:id="@+id/inProgress"
            layout="@layout/layout_progress_dialog_rummy"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:visible="@{viewModel.isLoading()}"
            app:layout_constraintTop_toTopOf="@id/lContainer"
            app:layout_constraintBottom_toBottomOf="@id/lContainer"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
